import type { StoreItem } from '../../types/store'

export const storeService = {
  async getItems(): Promise<StoreItem[]> {
    // TODO: Implement the logic to fetch store items from the backend API
    return []
  },
}
